// 이 프로젝트는 더 이상 vinext/Cloudflare Workers로 빌드하지 않고
// 표준 Next.js(`next dev`/`next build`)로 Vercel에 배포한다.
// 이 파일은 더 이상 쓰이지 않지만(어떤 스크립트도 vite를 호출하지 않음),
// 관련 패키지(vite, vinext, @cloudflare/vite-plugin)를 지우면서
// 파일 자체를 삭제할 수 없는 환경 제약 때문에 내용만 비워둔다.
export {};
